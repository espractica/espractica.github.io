{{#each licenses}}
<section>
		<img src="{{this.img}}" alt="{{this.title}} logo">
		<h2>{{this.title}}</h2>
		<p>{{this.description}}</p>
		<p> <span>License:</span>{{this.description}}</p>	
</section>
{{/each}}